{% extends 'base.html.twig' %}

{% block title %}Hello ZoneTechnicienController!{% endblock %}

{% block body %}

<h1>TYPOLOGIE</h1>

<h2>Ajout</h2>

<div>
    {{ form_start(zoneTechnicien_form) }}

    {{ form_widget(zoneTechnicien_form) }}

    <button type="submit" class="btn btn-success">Ajout</button>

    {{ form_end(zoneTechnicien_form) }}
</div>

<h2>Listes</h2>

<table class="table">
    <tr>
        <th>Nom</th>
        <th>Description</th>
    </tr>
    {% for zoneTechnicien in zoneTechniciens %}
    <tr>
        <td>{{ zoneTechnicien.nom }}</td>
        <td>{{ zoneTechnicien.description }}</td>
    </tr>
    {% endfor %}
</table>

{% endblock %}
