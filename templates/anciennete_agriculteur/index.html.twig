{% extends 'base.html.twig' %}

{% block title %}Hello AncienneteAgriculteurController!{% endblock %}

{% block body %}

<h1>ANCIENNETE AGRICULTEUR</h1>

<h2>Ajout</h2>

<div>
    {{ form_start(ancienneteAgriculteur_form) }}

    {{ form_widget(ancienneteAgriculteur_form) }}

    <button type="submit" class="btn btn-success">Ajout</button>

    {{ form_end(ancienneteAgriculteur_form) }}
</div>

<h2>Listes</h2>

<table class="table">
    <tr>
        <th>Nom</th>
        <th>Description</th>
    </tr>
    {% for ancienneteAgriculteur in ancienneteAgriculteurs %}
    <tr>
        <td>{{ ancienneteAgriculteur.nom }}</td>
        <td>{{ ancienneteAgriculteur.description }}</td>
    </tr>
    {% endfor %}
</table>

{% endblock %}